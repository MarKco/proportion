<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Host activity for Compose tests that need the real Hilt graph. Debug builds only. -->
        <activity
            android:name="com.ilsecondodasinistra.proportion.HiltTestActivity"
            android:exported="false"
            android:theme="@style/Theme.ProPortion" />
    </application>
</manifest>
