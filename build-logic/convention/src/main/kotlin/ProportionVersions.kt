/** Single source of truth for the SDK levels named in the design spec. */
internal object ProportionVersions {
    const val COMPILE_SDK = 36
    const val TARGET_SDK = 36
    const val MIN_SDK = 26
}
